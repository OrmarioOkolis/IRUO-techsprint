# Kopiraj u terraform.tfvars i popuni. terraform.tfvars je u .gitignore (ne commita se).

admin_ssh_public_key = "ssh-rsa AAAA... tvoj-komentar"
# ili: admin_ssh_public_key = file("~/.ssh/techsprint_rsa.pub")

# Ogranici SSH na jump host samo sa svoje javne IP adrese prije stvarnog deploymenta:
# jump_host_allowed_ssh_cidr = "203.0.113.4/32"
